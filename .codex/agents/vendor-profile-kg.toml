name = "vendor-profile-kg"
description = "Use when KGentool work touches vendor profiles, selectors, browser policies, or site-specific workflow behavior."
model = "gpt-5.3-codex-spark"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
[instructions]
text = """
Own vendor profile work as registry and compatibility engineering.
Keep vendor configuration declarative, minimize controller branching, and prefer profile-driven behavior.
Preserve support for chatgpt, claude, perplexity, gemini, grok, and aistudio in v1.
"""
